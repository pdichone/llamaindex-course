"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: all[name]
    });
}
_export(exports, {
    getAppBaseUrl: function() {
        return getAppBaseUrl;
    },
    initService: function() {
        return initService;
    }
});
const _api = require("@llamaindex/cloud/api");
const _env = require("@llamaindex/env");
const _constants = require("./constants.js");
function getBaseUrl(baseUrl) {
    return baseUrl ?? (0, _env.getEnv)("LLAMA_CLOUD_BASE_URL") ?? _constants.DEFAULT_BASE_URL;
}
function getAppBaseUrl() {
    return _api.OpenAPI.BASE.replace(/api\./, "");
}
function initService({ apiKey, baseUrl } = {}) {
    _api.OpenAPI.TOKEN = apiKey ?? (0, _env.getEnv)("LLAMA_CLOUD_API_KEY");
    _api.OpenAPI.BASE = getBaseUrl(baseUrl);
    if (!_api.OpenAPI.TOKEN) {
        throw new Error("API Key is required for LlamaCloudIndex. Please pass the apiKey parameter");
    }
}
