"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "DocxReader", {
    enumerable: true,
    get: function() {
        return DocxReader;
    }
});
const _schema = require("@llamaindex/core/schema");
const _mammoth = /*#__PURE__*/ _interop_require_default(require("mammoth"));
const _type = require("./type.js");
function _interop_require_default(obj) {
    return obj && obj.__esModule ? obj : {
        default: obj
    };
}
class DocxReader extends _type.FileReader {
    /** DocxParser */ async loadDataAsContent(fileContent) {
        // Note: await mammoth.extractRawText({ arrayBuffer: fileContent });  is not working
        // So we need to convert to Buffer first
        const buffer = Buffer.from(fileContent);
        const { value } = await _mammoth.default.extractRawText({
            buffer
        });
        return [
            new _schema.Document({
                text: value
            })
        ];
    }
}
