"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "storageContextFromDefaults", {
    enumerable: true,
    get: function() {
        return storageContextFromDefaults;
    }
});
const _schema = require("@llamaindex/core/schema");
const _env = require("@llamaindex/env");
const _imageembedmodel = require("../internal/settings/image-embed-model.js");
const _constants = require("./constants.js");
const _SimpleDocumentStore = require("./docStore/SimpleDocumentStore.js");
const _SimpleIndexStore = require("./indexStore/SimpleIndexStore.js");
const _SimpleVectorStore = require("./vectorStore/SimpleVectorStore.js");
async function storageContextFromDefaults({ docStore, indexStore, vectorStore, vectorStores, storeImages, persistDir }) {
    vectorStores = vectorStores ?? {};
    if (!persistDir) {
        docStore = docStore ?? new _SimpleDocumentStore.SimpleDocumentStore();
        indexStore = indexStore ?? new _SimpleIndexStore.SimpleIndexStore();
        if (!(_schema.ModalityType.TEXT in vectorStores)) {
            vectorStores[_schema.ModalityType.TEXT] = vectorStore ?? new _SimpleVectorStore.SimpleVectorStore();
        }
        if (storeImages && !(_schema.ModalityType.IMAGE in vectorStores)) {
            vectorStores[_schema.ModalityType.IMAGE] = new _SimpleVectorStore.SimpleVectorStore({
                embedModel: new (await (0, _imageembedmodel.getImageEmbedModel)())()
            });
        }
    } else {
        docStore = docStore || await _SimpleDocumentStore.SimpleDocumentStore.fromPersistDir(persistDir, _constants.DEFAULT_NAMESPACE);
        indexStore = indexStore || await _SimpleIndexStore.SimpleIndexStore.fromPersistDir(persistDir);
        if (!(_schema.ObjectType.TEXT in vectorStores)) {
            vectorStores[_schema.ModalityType.TEXT] = vectorStore ?? await _SimpleVectorStore.SimpleVectorStore.fromPersistDir(persistDir);
        }
        if (storeImages && !(_schema.ObjectType.IMAGE in vectorStores)) {
            vectorStores[_schema.ModalityType.IMAGE] = await _SimpleVectorStore.SimpleVectorStore.fromPersistDir(_env.path.join(persistDir, _constants.DEFAULT_IMAGE_VECTOR_NAMESPACE), new (await (0, _imageembedmodel.getImageEmbedModel)())());
        }
    }
    return {
        docStore,
        indexStore,
        vectorStores
    };
}
