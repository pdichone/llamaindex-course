import type { NodeWithScore } from "@llamaindex/core/schema";
import type { BaseNodePostprocessor } from "./types.js";
export declare class MetadataReplacementPostProcessor implements BaseNodePostprocessor {
    targetMetadataKey: string;
    constructor(targetMetadataKey: string);
    postprocessNodes(nodes: NodeWithScore[]): Promise<NodeWithScore[]>;
}
